.\deploy.ps1              # to'liq: build + yuklash + almashtirish + tekshirish
.\deploy.ps1 -SkipBuild   # qayta build qilmasdan, tayyor dist/ ni yuboradi
.\deploy.ps1 -Rollback    # oldingi versiyaga qaytaradi
